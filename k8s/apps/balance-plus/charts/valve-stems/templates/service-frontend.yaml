apiVersion: v1
kind: Service
metadata:
  name: {{ include "valve-stems.fullname" . }}
  labels:
    {{- include "valve-stems.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valve-stems.selectorLabels" . | nindent 4 }}

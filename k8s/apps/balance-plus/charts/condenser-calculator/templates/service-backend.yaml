apiVersion: v1
kind: Service
metadata:
  name: {{ include "condenser-calculator.fullname" . }}
  labels:
    {{- include "condenser-calculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "condenser-calculator.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "condenser-calculator.fullname" . }}-backend
  labels:
    {{- include "condenser-calculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "condenser-calculator.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
